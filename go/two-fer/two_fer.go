package twofer

import "fmt"

// ShareWith interpolates a name into a sentence.
func ShareWith(name string) string {
	if name == "" {
		name = "you"
	}
	return fmt.Sprintf("One for %s, one for me.", name)
}
